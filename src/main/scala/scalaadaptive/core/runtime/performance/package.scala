package scalaadaptive.core.runtime

/**
  * Created by Petr Kubat on 7/19/17.
  *
  * Package with a set of simple classes used for tracking the selection and invocation process performance and
  * overhead. Used internally by the framework for the [[scalaadaptive.core.functions.statistics.StatisticsHolder]]
  * and [[scalaadaptive.analytics.AnalyticsData]].
  *
  * The main type used is the [[scalaadaptive.core.runtime.performance.PerformanceTracker]], implemented by the
  * [[scalaadaptive.core.runtime.performance.BasicPerformanceTracker]].
  *
  */
package object performance {

}
